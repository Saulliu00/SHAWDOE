{
  "name": "@kindwords/api-handlers",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kindwords/types": "*",
    "@kindwords/utils": "*",
    "@kindwords/llm-pipeline": "*",
    "@kindwords/data-access": "*",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
